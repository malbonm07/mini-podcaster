import styled from 'styled-components';

const AppWrapper = styled.div`
    max-width: 1100px;
    margin-left: auto;
    margin-right: auto;
    min-height: 100vh;
    padding-bottom: 20px;
`;

export default AppWrapper;