const PROXIES = Object.freeze({
    CORS_ANYWHERE: 'https://cors-anywhere.herokuapp.com/',
    THINGPROXY: 'https://thingproxy.freeboard.io/fetch/'
})

const CORS_PROXY = PROXIES.THINGPROXY;

export default CORS_PROXY;